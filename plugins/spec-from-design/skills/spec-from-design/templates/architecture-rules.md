# {서비스명} 아키텍처 규칙

## 레이어 구조

> existing-project: 코드에서 추출
> new-project: 프리셋에서 로드

| 레이어 | 패키지 | 역할 | 의존 가능 |
|--------|--------|------|----------|
| {레이어} | {패키지} | {역할} | {의존 방향} |

## 의존성 규칙

- {규칙 1: 예) domain은 infrastructure에 의존하지 않는다}
- {규칙 2}

## 컴포넌트 역할

{HLD Architecture 컴포넌트 테이블에서 도출}

| 컴포넌트 | 레이어 | 역할 | 기술 스택 |
|----------|--------|------|----------|
| {컴포넌트} | {레이어} | {역할} | {기술} |

## 모듈 구조

> existing-project: settings.gradle 등에서 추출
> new-project: 프리셋에서 로드

```
{모듈 트리}
```
