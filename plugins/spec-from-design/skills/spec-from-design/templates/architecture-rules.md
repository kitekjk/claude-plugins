# 아키텍처 규칙 (Architecture Rules)

> 생성 주체: design-analyzer
> 생성 시점: 파이프라인 ① 단계
> 참조 출처: HLD 아키텍처 결정 + 코드 구조 분석 (existing-project) 또는 preset (new-project)

---

## 레이어 구조

{이 프로젝트의 레이어 분류와 각 레이어의 역할}

| 레이어 | 경로 패턴 | 역할 |
|-------|---------|------|
| {레이어명} | `{패키지 경로 패턴}` | {역할 설명} |
| {레이어명} | `{패키지 경로 패턴}` | {역할 설명} |
| {레이어명} | `{패키지 경로 패턴}` | {역할 설명} |
| {레이어명} | `{패키지 경로 패턴}` | {역할 설명} |

## 의존성 방향

{레이어 간 허용/금지 의존성 방향 규칙}

### 허용

```
{레이어 A} → {레이어 B}: {허용 이유}
{레이어 B} → {레이어 C}: {허용 이유}
```

### 금지

```
{레이어 C} → {레이어 A}: {금지 이유}
{레이어 B} → {레이어 A}: {금지 이유}
```

### 의존성 역전 규칙

{Port/Adapter 패턴 또는 인터페이스 기반 역전이 적용되는 경우 기술}

- {규칙 1}
- {규칙 2}

## 레이어별 책임 규칙

### {레이어 A}

- {허용 책임}
- 금지: {이 레이어에서 해서는 안 되는 것}

### {레이어 B}

- {허용 책임}
- 금지: {이 레이어에서 해서는 안 되는 것}

### {레이어 C}

- {허용 책임}
- 금지: {이 레이어에서 해서는 안 되는 것}

## 트랜잭션 경계

{트랜잭션을 어느 레이어에서 관리하는지 규칙}

- 트랜잭션 시작: {레이어명} (`@Transactional` 위치)
- 읽기 전용: {레이어명} (`@Transactional(readOnly = true)`)
- 금지: {트랜잭션 경계 관련 금지 사항}

## 이벤트/메시지 규칙

{이벤트 발행 및 소비 위치 규칙}

- 이벤트 발행 위치: {레이어명}
- 이벤트 소비 위치: {레이어명}
- {기타 이벤트 관련 규칙}
